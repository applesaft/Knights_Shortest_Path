# knights_path
A Python program that finds the fewest knight moves between two squares on an 8×8 chessboard, using a recursive depth-first search with branch-and-bound pruning and a Manhattan-distance heuristic to order moves and speed up the search.
